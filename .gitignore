# build output
/bin/

# editor backups
*~

# local catalogs / test artifacts
/nbackup-catalog/
*.tar.zst

# the real config is local; only the example is tracked
/nbackup.yaml

# Claude Code local agent state is ignored, but checked-in commands are tracked.
.claude/*
!.claude/commands/

.idea/
nbackup-state/