# build output
/bin/

# editor backups
*~

# local catalogs / test artifacts
/nbackup-catalog/
*.tar.zst

# the real config is local; only the example is tracked
/nbackup.yaml

# Claude Code local agent state is ignored, but checked-in commands and skills are tracked.
.claude/*
!.claude/commands/
!.claude/skills/

.idea/
nbackup-state/

# Jekyll docs-site local preview artifacts (GitHub Pages builds from source)
/docs/_site/
/docs/.jekyll-cache/
/docs/.jekyll-metadata
/docs/Gemfile.lock
/docs/vendor/